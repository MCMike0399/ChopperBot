# Calendar capability (global)

> Topic doc — part of the [CLAUDE.md](../../CLAUDE.md) router. Read it BEFORE working in this area, and keep it current in the same change as any behavior change (doc protocol ①/② in the index).

Rewritten 2026-06-21 from per-user to a single **global** server calendar. Mods talk to it in the bound **input** channel; rendered month PDFs + a master ICS are published to a separate **output** channel.

- **Writing is mod-only, in code (added 2026-08-13).** The prompt always said "cualquier moderadorx de este canal", but nothing checked — the real guarantee was "whoever can post in the channel", and an event created here is _published_ state (month PDF, ICS, the Discord events list, the next morning's announcement to the whole server). `buildTurn` now calls `isModTurn()` (approver role or Administrator — same list as event_intake, see docs/framework.md); a non-mod gets the **read-only slice** (`calendar_list_upcoming` / `calendar_search_events` / `calendar_get_event`, no publisher, no Discord-event syncer, attachments not offered as banners) plus a short "solo consulta" prompt, and `allowWrite:false` re-refuses at the point of effect. Fails closed. Asking what's coming up stays open to everyone in the channel.
- **`when` on list/search payloads (added 2026-08-25).** Each occurrence includes `local_date` (CDMX YYYY-MM-DD) and `when` (`hoy`/`mañana`/`después`) so "¿qué hay mañana?" does not depend on the model subtracting UTC-6 from `start_at_iso` (an 8pm CDMX event is the next UTC date). Live miss: general_chat at 11:14 CDMX on Tue 25 answered "mañana (martes 25)" because its prompt only injected the UTC timestamp (17:14 the same day) and Cooperativas' ISO is `2026-08-26T02:00:00Z`. Values are Spanish (not `today`/`tomorrow`) so the payload does not prime Spanglish — live the same evening, "Estoy emoción-ed". `renderTemporalAwareness` now also names hoy/mañana/ayer by local date (not just the weekday) and is used by general_chat, not only calendar/event_intake. Covered by `__tests__/time.test.ts` plus the list_upcoming payload case in `calendar.test.ts`.
- **`discord_event_url` on list/search/get (added 2026-08-25).** When a row has `discord_event_id` and the tool source knows the guild, the payload also carries `https://discord.com/events/<guild>/<id>` so general_chat can answer "dónde reservo / cómo me apunto" with the real RSVP link instead of inventing a ticket. Omitted when there is no link (tests without `guildId`, unlinked rows). Covered by the list_upcoming case in `calendar.test.ts`.
- **Two channels, two roles.** The capability is bound to the INPUT channel via the normal routing table (`config_bindings action:bind … capability:calendar`). The OUTPUT channel is a calendar-specific setting: seeded on first boot from `CALENDAR_OUTPUT_CHANNEL_ID` into `calendar_settings` (single row), then the DB value wins (change it from the config channel: `config_calendar action:set_output_channel`). Resolution order at runtime: DB setting → `CALENDAR_OUTPUT_CHANNEL_ID` → none (publish becomes a no-op reported as `published.ok=false`).
- **Global store** (`store.ts`, migration **v5**): `calendar_events` dropped per-user scoping (`created_by` is now attribution only — any mod sees/edits any event). v5 also adds `calendar_published` (tracks the Discord message id per artifact so we EDIT in place) and `calendar_settings`. Recurrence (`daily`/`weekly`/`monthly` + optional `recurrence_until`) lives in `recurrence.ts`; `expandOccurrences` is reused by the snapshot, the renderer, and the ICS.
- **Bounded recurring series** (`recurrence_count` / `recurrence_until_iso`, added 2026-08-03, **no migration**). A series can end. `recurrence_count` ("cada martes, 4 veces") and `recurrence_until_iso` ("hasta el 31 de agosto") are two spellings of the same bound and are **mutually exclusive** (passing both is a tool error, not a silent pick); both resolve at write time into the single pre-existing `recurrence_until` column via the pure `untilFromCount()` in `recurrence.ts` — which is why no schema change was needed and the renderer / ICS `UNTIL=` / `expandOccurrences` all kept working untouched. `recurrence_count: 1` collapses to a one-off rather than a degenerate 1-instance series. On update (scope `series`) a count is measured against the series' **effective** start and frequency, so a single call that also moves the series or changes its rhythm re-bounds against what it will BE, not what it was; `recurrence_until_iso: null` clears the bound, and dropping `recurrence_freq` clears it too. Tool results carry `occurrence_count` / `recurrence_until_local` / `recurrence_open_ended` so confirmations are concrete ("semanal, 4 sesiones, la última el Tue Jul 28").
   - **Why this exists (real failure, journal 2026-07-10):** a mod booked a weekly book club and the bot created **four separate one-off rows** (#14/#15/#16/#17, plus #11 and later #19 — six rows for one series) because the tools only offered "weekly → forever" _and_ the prompt then said `NUNCA preguntes "¿hasta cuándo se repite?"`. That directive is now **reversed**: the prompt asks about the range **once, non-blocking**, treats "todo julio"/"por un mes"/"6 sesiones" as a range to resolve, and still creates an **indefinite** series when the mod has no end date (a permanent asamblea is legitimate — 3 such series exist live). Scenes 8–10 of `scripts/calendar-bedrock-smoke.ts` replay this against the real model; the hard check is "ONE row, bounded".
   - **Theming one session is its own tool — `calendar_set_session_theme`** (added 2026-08-13, v1.19.0). The community's weekly clubs decide the night's movie/theme one or two days before, often by just sending the flyer. That is an occurrence override (title + optional description + optional same-day retime), and the generic update tool already did it — but it asks the model to assemble scope + anchor + ISO math, and live on 2026-08-13 it instead created a duplicate "Cine Club: Persepolis" series for the week's film. The narrow tool takes `id` + `occurrence_date_iso` + `title` (+ optional `description`/`start_at_iso`/`location`), refuses non-recurring masters and cross-day retimes, then republishes and **creates-or-refreshes** the Discord event (an unlinked series gets one — the theme being decided late is exactly when the Discord event doesn't exist yet). The system prompt carries the matching rule: "la peli/tema de la semana NO es un evento nuevo" — search the ACTIVITY name ("club de cine"), never the movie title, and never create. Covered by `__tests__/set-session-theme.test.ts`; drive it by hand with `scripts/calendar-tool.ts calendar_set_session_theme '<json>'`.
- **Recurring-event exceptions** (migration **v6**, `calendar_event_overrides`, added 2026-06-21): edit or cancel a SINGLE occurrence, or "this and following". A row overrides/cancels the occurrence whose ORIGINAL anchor time is `occurrence_start_at` (NULL field = inherit); `expandOccurrences` applies them (cancel → skip, retime → shifted, same-day only). The tools `calendar_update_event`/`calendar_delete_event` take a **`scope`**: `series` (default; also for one-offs), `occurrence` (just `occurrence_date_iso`), or `following`. "Following" is a **series split** (no override rows): the original master's `recurrence_until` is truncated to just before the split and a NEW master is created from the split point (delete-following just truncates). Splitting at the first occurrence collapses to a whole-series edit. A whole-series `start_at`/`recurrence_freq` change wipes the master's overrides (the rhythm changed). The prompt makes the model ASK "¿solo ese día, este y los siguientes, o toda la serie?" when scope is ambiguous, and the tool result carries `updated_scope`/`deleted_scope`.
- **Follow-up conversation** needs no new infra — the existing reply-chain `buildHistory` already threads multi-turn context. The system prompt (`capability.ts`) drives it: require title + start date/time before creating, ask one concise question at a time for missing/ambiguous bits (location, "¿se repite?", and for a series "¿cuántas sesiones o hasta cuándo?"), Spanish-first. Only title + time + (date or cadence) are blocking; everything else asks at most once and never holds up the create.
- **PDF rendering** (`render.ts` + `template-geometry.ts`). The 7 templates in `calendar/` (Jun–Dec 2026, **not gitignored** → they deploy with the repo and are read at runtime from the repo root) are Canva designs with the grid, day numbers and branding already drawn. `scripts/calibrate-calendar-templates.ts` bakes per-month grid geometry into `template-geometry.generated.ts` (poppler, **dev-time only**): **rows** come from the day-number text bounding boxes (`pdftotext -bbox`); **columns** come from detecting the true vertical grid lines in a grayscale raster (`pdftoppm -gray`), because the day numbers are right-aligned and inset ~8px from the real cell border — using the text edge made event blocks bleed left across the gridline (fixed 2026-06-21). The horizontal grid is identical across templates, so the calibration applies a **consensus** column set to all 7 (one template's line-detection miss can't desync it). Row pitch differs (~97pt for 5-row months, ~80pt for the 6-row Aug/Nov). Each day's (row,col) is computed **arithmetically** (Monday-first, fixed UTC-6) by the pure grid math in `grid.ts`, which is shared by the renderer and the publisher (so both agree on placement). The renderer (pdf-lib, standard Helvetica) overlays compact red "event blocks" (bold time + title) into the cell; recurring events expand so a weekly series lands in every week and every month. Text is sanitized to WinAnsi (emoji/CJK stripped, smart-punctuation folded) so pdf-lib never throws. **Re-run the calibration script if the templates are ever replaced.**
   - **Cell packing is a whole-cell plan, not a per-event greedy loop** (`cell-layout.ts`, added 2026-08-15, v1.21.0). The old loop laid out event #1 as generously as it could and then asked whether #2 still fit — which is exactly what starved it. Live on Agosto 2026, **Aug 10** (`#31` + `#28`) and **Aug 16** (`#36` + `#30`) each showed one event and "+1 más" with a whole chip of empty space visible below: the first title took 2 lines (24 pt) + a 2.5 pt gap, leaving **14.48 pt** where a one-line chip needs **14.5**. `planCell()` (pure, injected `TextMeasurer`, so it's testable without pdf-lib) instead: ① gives every event **one line first** — maximizing how many events are _shown_, because a hidden event is invisible while a clipped title is still a legible event; ② hands the leftover height back out as extra title lines in fair round-robin passes (everyone gets a 2nd line before anyone gets a 3rd); ③ retries on a 5-step **density ladder** that tightens padding before it touches the type, and accepts a denser tier **only when it shows strictly more events** — so type never shrinks merely to un-clip a title and an ordinary day keeps the design's default look (base 8 pt on 5-row months, 7 pt on the 6-row ones, floor 5.5 pt); ④ reserves the "+N más" line's height up front instead of drawing it into whatever is left. Result on the real board: 3 events fit a 6-row cell (4 in a 5-row one) where 1–2 used to, and "+N más" now means the day is genuinely full. Covered by `__tests__/cell-layout.test.ts` (packing invariants against a deterministic fake measurer — including `blocks + hidden === input` for 1…12 events) plus a real-geometry/real-Helvetica case in `render.test.ts`.
   - **The MediaBox origin bug this uncovered.** Every Canva template's page box is `[0, 7.92, 1440, 817.92]`, but the bbox→pdf-lib flip was `pageHeight − bboxY` with an implied origin of 0 — so **every chip in every month had been drawn 7.92 pt too low** since the renderer shipped. It was invisible while cells held one chip (the 7 pt bottom pad absorbed it) and became a _crossed week divider_ the moment cells packed full. `cellBox()` now takes `pageOriginY` and the renderer passes `page.getMediaBox().y`. Verified by measuring a 144-DPI raster of the composed page: the week-of-Aug-10 band runs 420 → 499.5 in bbox space and both chips now land at 456 → 480 and 482.5 → 497. `render.test.ts` pins both the MediaBox value of all 7 templates and the "cell sits between the printed dividers" property (asserting the origin-less call still fails it).
   - **Eyeball it after any change here:** `npx tsx scripts/render-calendar-sample.ts` now carries a **density ladder** (2/3/4/6 events on consecutive August days, 3/5 in September) so the tiers can be read straight off one rendered week, and `npx tsx scripts/preview-calendar-board.ts --render` renders the REAL board. Unit tests can't see a chip cross a gridline — crop the PNG and look (`pdftoppm -png -r 220 -x … -y … -W … -H … -singlefile <pdf> out`).
- **PNG hero** (`raster.ts`): Discord shows a PDF as a non-previewing download card, so the publisher rasterizes each composed month PDF to a **PNG via `pdftoppm` at 150 DPI** (3000×1688, ~0.65 MB) and attaches **only the PNG** so Discord renders it inline (the PDF is dropped — cleaner cards; re-add it to the payload if print-quality downloads are wanted again). This is a **runtime** poppler dependency (installed at `/usr/bin`, on the systemd PATH); if it's ever missing, raster throws and the publisher falls back to attaching the PDF so the month still posts (`calendar.raster_failed` logged once).
- **ICS** (`ics.ts`): one VEVENT per master with RRULE for recurring series (never pre-expanded), a fixed-offset `VTIMEZONE America/Mexico_City` (-0600), stable UIDs (`chopperbot-cal-<id>@revolucionz`) so re-importing updates rather than duplicates, RFC-5545 escaping + 75-octet line folding. Per-occurrence exceptions map to standard iCal: a cancelled occurrence → `EXDATE` on the master VEVENT; a retimed occurrence → a second VEVENT with the same UID + `RECURRENCE-ID` (original anchor) and the new DTSTART/SUMMARY. (A downloaded `.ics` is a one-time import snapshot — there's no hosted webcal URL, so the copy refreshes on the single ICS message but members re-import to pick up changes.)
- **Publishing** (`publisher.ts`, `OutputChannelPublisher`): every mutation (plus bot startup and the month-rollover watcher, both via `CalendarCapability`) calls **`reconcile()`** — a single idempotent op that makes the output channel match the DB. reconcile renders each desired month (PDF + PNG hero, concurrently), edits its tracked message in place (`calendar_published`), **deletes** any tracked card no longer desired, and refreshes the single ICS message. Per month: one message = **PNG hero** (PDF only as a raster fallback). The ICS is a **single dedicated message**. Best-effort: failures never fail the DB mutation; the tool result carries a `published` summary (`posted`/`removed`) the model relays. `calendar_publish` just forces a `reconcile()` on demand.
- **The desired card set** is the pure, unit-tested `desiredMonthKeys(events, nowMs)` (exported from `publisher.ts`). The board is a **live board, not an archive**: the **current month always** (even with zero events — the caption reads "sin eventos por ahora"), plus every **FUTURE** month containing a **one-off** event, and **never a past month**. Two invariants it encodes:
   - A **recurring series never spawns future-month cards** (2026-06-21 bug: one weekly event blasted all 7 months / 8 messages). It shows on the current month's card, and reaches future months as they become current; meanwhile it's in the ICS.
   - **Past months are pruned** (changed 2026-08-03). Previously the set was "every month with a one-off + current month", so a past month with a one-off stayed desired _forever_ — on 2026-08-03 the live board still showed June and July. "Current month always" also matters mechanically: it's what guarantees the rollover trigger below resolves.
- **Month-rollover auto-publish** (added 2026-08-03, `CalendarCapability`). `reconcile()` used to run **only** on a mutation or at boot, so a month rollover on a long-running process published nothing: the bot had been up since Jul 29 and on Aug 3 the board still showed June+July with no August card. `start()` now also arms a `setInterval` (`ROLLOVER_CHECK_MS`, 10 min — one SQLite read per tick) that calls `checkMonthRollover()`.
   - The trigger is the pure `monthPublishAction(currentMonthKey, isPublished)` and is deliberately **stateless — no "last rollover" timestamp and no migration**. Since the board always keeps a card for the current month, a **missing `pdf:<currentMonth>` row in `calendar_published` means "this month isn't published yet"**. Consequences that are tested: a restart can't double-post (the row lives in SQLite), and a failed publish leaves the row absent so the next tick retries by itself.
   - Rollover also **prunes the previous month's card** (same reconcile), so Sep 1 replaces the August card rather than stacking on it.
   - Boot logs `calendar.rollover_watch_started {check_every_min, current_month, verdict}` — the watcher is otherwise silent until a month actually turns, so this is the only way to confirm it's armed without waiting for the 1st. A rollover logs `calendar.rollover.publishing` then `calendar.rollover_reconcile`.
   - **Templates are finite (Jun–Dec 2026)**, so this is where they run out. If the current month has no template the watcher can't publish → `sendAdminAlert` to the config channel (once per process per month, `calendar.alert`). And after each successful rollover publish it checks the **next** month's template and alerts if it's missing — a month of warning to draw the Canva page, instead of a board that silently stops updating.
- **Daily same-day announcement** (added 2026-08-05, v1.8.0; `announce.ts` = pure policy, `announcer.ts` = orchestration, `match.ts` = matching, `discord-events.ts` = the Discord API, `announce-settings.ts` = settings resolution, migration **v7**). Every morning at `CALENDAR_ANNOUNCE_HOUR` (10:00 local) the bot posts today's events to a **third** channel — the community `📣│anuncios` (`CALENDAR_ANNOUNCE_CHANNEL_ID`, live: `1435843684628172953`) — in the community's own voice, with the **Discord scheduled-event link** so members can RSVP. Armed by a second `setInterval` in `start()` (`ANNOUNCE_CHECK_MS`, 5 min) and run once immediately at boot.
   - **Three channels now, three roles:** INPUT (routing table, where mods talk) → OUTPUT (`CALENDAR_OUTPUT_CHANNEL_ID`, month PDFs + ICS) → ANNOUNCE (`CALENDAR_ANNOUNCE_CHANNEL_ID`, the daily heads-up). All three seed-from-env-then-DB-wins; manage live with `config_calendar action:set_announce_channel` / `set_announce_mentions`.
   - **It's a window, not an alarm.** `announcementsDue()` (pure) = "today's local date, at/after the announce hour, not yet started, not in the ledger". So a boot at 11:00, a 5-min tick, and a mod booking a same-day event at 3pm all produce exactly ONE post — the SQLite ledger `calendar_announcements` decides, never the clock. An event that already started is skipped rather than announced late ("hoy a las 8pm" posted at 8:40 reads as broken).
   - **Matching the Discord event is the hard part, and it's a funnel** (`match.ts`, pure + tested): **stored link → deterministic score → the model**. Admins create the Discord events by hand in their own words ("Rosario Castellanos | Club de poesía" in the calendar vs "Club de Poesía: Rosario Castellanos" on Discord), so titles rarely match literally but the start time almost always does. Score = `0.6 × symmetric token-Dice(title) + 0.4 × time-closeness`, gated to a ±26 h window; auto-accept needs score ≥ 0.62 **and** a title score ≥ 0.34 **and** a clear runner-up margin. Time alone must never carry a match — two different activities in different rooms at 8pm is exactly how the community schedules. Ambiguous → the model arbitrates (`effort:'medium'`, Kimi) and is told "es mucho peor equivocarse que decir null"; its answer is verified against the candidate ids and nullish-string tokens are normalized (the same defence the IG classifier needed). The accepted link is **persisted** to `calendar_events.discord_event_id`, so matching costs at most one model call per event, ever — not one per day. A stale link (admin deleted the event) is detected and cleared, then re-matched.
   - **Model calls are gated to what's actually being announced.** `resolveTarget(..., { allowModel })` is true only for occurrences in `due`. Everything else in the window is inspected only for the nudge check, which needs "is there plausibly an event?" — answerable deterministically. Without this gate an unmatched event costs a model call **every 5 minutes forever** (~288/day; observed live on `Club de cine`). An ambiguous-but-unarbitrated target is flagged `maybeLinked` so the nudge stays quiet instead of nagging about an event that probably exists.
   - **Exactly-once posting is enforced against Discord, not just against the ledger — and it is PREVENTED, not only repaired** (prevention added 2026-08-11, v1.16.2). Everywhere else in this bot a duplicated send is a harmless repeated reply; here it's a duplicated @-ping to the whole server, so the send is defended twice over:
      - **Prevention — `nonce` + `enforceNonce` on the create.** `POST /channels/{id}/messages` is not idempotent by default, and `@discordjs/rest` defaults to `timeout: 15_000, retries: 3`: on this Pi's uplink the request regularly outlives 15 s **after Discord already created the message**, so the abort-and-retry lands a second (and third) copy. The payload now carries Discord's own idempotency key — the pure, length-bounded `announceNonce(eventId, occurrenceStartMs)` (`announce.ts`, base36, ≤25 chars, Discord's cap) plus `enforceNonce: true` — so a retried create **returns the existing message instead of making another**. Derived from the occurrence's identity rather than randomly, so it also collapses two _different_ attempts at the same announcement (overlapping ticks, a restart mid-post); a deliberate repost (`ignoreLedger`) salts it with `nowMs` so it is not swallowed as a duplicate of the morning's post. Verified live against the real API: the same nonce sent twice deduped **text-only, multipart-with-attachment (the flyer path), and still after 60 s** — comfortably longer than the ~45 s retry burst.
      - **Repair — the sweep, now a backstop.** `sendExactlyOnce()` still notes the channel's newest message id, sends, then fetches what actually landed and **deletes its own duplicates, keeping the earliest**, covering anything the nonce doesn't (a copy created outside the dedup window). It also repairs the inverse case (send rejects but the message exists → adopt the orphan instead of re-announcing next tick).
      - **Why the sweep alone was not enough (2026-08-11 triage).** It runs _after_ the copies exist, and **deleting a message does not retract its notification** — every landed copy had already pinged `Usuarix`. The live rate was far worse than the journal suggested, because admins were cleaning up too: `duplicatesRemoved` read 2 (Aug 6), 1 (Aug 8), 0 (Aug 11) — but the guild audit log showed an admin hand-deleting ChopperBot messages in the announce channel on Aug 5, 6 **and Aug 11 at 10:05:11, `count=2`, nine seconds before the bot's own sweep ran at 10:05:20**. So Aug 11 was a _triple_ post that logged `duplicatesRemoved: 0`. Snowflake deltas between the copies — **14.8 s, 14.9 s, 29.9 s** — are exactly one and two REST timeouts, which is what identified the retry as the mechanism. **Lesson for triage: `duplicatesRemoved` is a lower bound; cross-check `fetchAuditLogs({type: MessageDelete})` before believing a clean journal.**
   - **The announce pass is serialized against itself** (`CalendarCapability.runAnnouncer`, added 2026-08-11). A pass reads the ledger at the start and writes it only after the model has written the text and Discord has accepted the post — observed live at **~5 minutes** (a 4 m 15 s model call plus a 45 s retrying send) against a **5-minute** tick, i.e. the next tick could start, see "not announced yet", and post the same thing again. An in-flight pass now makes the next tick a no-op (`calendar.announce_tick_skipped_busy`, debug) rather than a second announcement. The nonce would also catch this, but only inside Discord's dedup window; the guard is the deterministic half.
   - **The text is model-written, the mechanics are not.** The writer prompt carries condensed real posts by this community's admins as style anchors (the voice is the whole reason to use a model) and is **forbidden** from writing mentions or URLs — `appendEventLink` + `prefixMentions` add those deterministically, exactly like event_intake's mod ping. A failed or suspiciously short model reply falls back to `renderFallbackAnnouncement()`: an outage costs the message its _style_, never the heads-up.
   - **The announcement does not attach the flyer** (changed 2026-08-20). The post is text + the Discord scheduled-event URL. Discord's own event embed already shows the cover, and attaching the same image duplicated it (live 2026-08-19: poetry-club announcement with the banner twice). No linked event → text-only, as before.
   - **Mentions default to the member role, not `@everyone`.** `CALENDAR_ANNOUNCE_MENTIONS` accepts role snowflakes and the literal token `everyone`; the live deploy pings `Usuarix` (`1436225305898389604`) — what admins use most. `allowedMentions.parse` is explicit, so `@everyone` can only fire when it was actually configured. The setting distinguishes **never configured** (SQL NULL → env seed applies) from **configured empty** (`[]` → ping nobody), so `set_announce_mentions ""` isn't undone by the next restart.
   - **Mod nudge for a missing Discord event.** Events today/tomorrow with no Discord event get ONE message in the calendar INPUT channel (found via the router; falls back to `sendAdminAlert`), pinging the approver roles — read from `event_intake`'s own setting so "who may approve" and "who gets nudged" can't drift. It deliberately passes `canMentionAny: false`: a role left unmentionable is admins saying "don't ping this one", and housekeeping must not override that (live: 5 approver roles, only 2 mentionable). One nudge per occurrence, tracked in the same ledger. Since **v1.16.0** each line carries the **`#id`** and the ask names it concretely ("respondan a este mensaje con _crea el evento de Discord del #28_") — the old text told mods to say `#<id>` while printing no id anywhere, a placeholder they couldn't resolve.
   - **Permissions — the thing that actually blocks this.** Reading Discord scheduled events needs no intent (plain REST via `guild.scheduledEvents.fetch()`), **but the list silently omits events tied to voice/stage channels the bot can't `ViewChannel`** — indistinguishable from "no event exists". Before `Gestionar eventos` was granted the bot saw **1 of 3** events (Asamblea-Z, Sala de Club de Poesía and Sala de Cineclub are all role-gated). Creating events needs `ManageEvents` too. `diagnoseEventAccess()` reports both, and `createScheduledEvent` returns a typed `missing_permission` carrying the Spanish fix rather than failing silently.
   - **Verify:** `npx vitest run src/capabilities/calendar` (`announce.test.ts` timing/ledger/message policy + the `announceNonce` length/stability bounds, `match.test.ts` the matcher incl. the real admin-rewording pairs, `announcer-dedup.test.ts` both halves of exactly-once — the duplicate-send repair with a fake channel that duplicates like Discord did, and the nonce prevention with a fake that honours `enforce_nonce` like Discord does, `announce-store.test.ts` migration v7, `discord-events.test.ts` venue reporting + the external→voice conversion against a fake that rejects stale metadata the way Discord does) plus `npx vitest run src/discord/__tests__/history.test.ts` for the folded-nudge context plus the live, read-only `npx tsx scripts/verify-calendar-announce.ts` (prints the permission diagnosis, the visible Discord events, and the exact message it WOULD post; `--post` to really announce, `--repost` to ignore the ledger). Journal: `calendar.announce_watch_started`, `calendar.announce.matched_auto` / `matched_model`, `calendar.announce.posted`, `calendar.announce.duplicate_send_repaired`, `calendar.announce.nudge_posted`, `calendar.announce_tick`.
- **On-demand announcements — "anúncialo en eventos, general y foro poesía"** (added 2026-08-18, v1.24.0; `broadcast.ts` = pure policy, `broadcast-channels.ts` = the Discord half, two mod tools, migrations **v9→v10**; one-shot publish + Usuarix default v1.28.0). A mod can now tell the bot to publish an event's announcement **now**, in the channels they name. **"Anúncialo / publícalo" is the confirmation** (`publish_now: true` posts in the same `calendar_draft_announcement` call). A later "sí" / "sip" / "QUE SI" spends `calendar_send_announcement` and **must not re-draft**. The preview-then-yes path is only when they asked to *see* the text first ("redáctame", "¿cómo quedaría?"). Live miss 2026-08-31 (Darko, _"anuncia el evento de hoy en general"_ → draft + "¿lo publico así?" → "si" drafted again → they had to yell QUE SI): send used to refuse a garbage token with no `tool_call` log, so the model drafted a second time. A missing/garbage token now falls back to the newest pending draft of **this** channel.
   - **Why:** live, in the calendar channel, a mod asked _"ayúdame a publicar un anuncio que diga bandaaaa, mañana a las 8pm tendremos Círculo de poemas propios"_. The bot found the event, confirmed its Discord event, and explained that the automatic 10:00 AM post covers it — a correct answer to that question. The follow-up, _"así está perfecto solo anúncialo tanto en eventos como en general y foro poesía"_, it structurally could not do: the announcer knows exactly ONE channel (`CALENDAR_ANNOUNCE_CHANNEL_ID`) and only fires on the day of, on its own schedule. The prompt now says in as many words that **the automatic announcement is never an answer to "publica esto"**.
   - **Two tools, because a post to the community cannot be undone** (deleting it does not retract its notification — the 2026-08-11 lesson above). `calendar_draft_announcement` writes the text, resolves the channels and parks a `token`; with `publish_now: true` it then posts in the same call. Without that flag it **posts nothing** and the model shows the text; `calendar_send_announcement` spends the token and posts **the stored text, byte-identical** — never a regenerated one. Single-use is SQLite's (`UPDATE … WHERE posted_at IS NULL`), so a repeated "sí, publícalo" reports "ya lo publiqué" instead of double-pinging; the token is burned _before_ the fan-out, making a crash mid-send under-deliver rather than double-post. Drafts expire in 30 min (`DRAFT_TTL_MS`) so "sí" tomorrow can't resurrect yesterday's _"mañana a las 8pm"_.
   - **Channel resolution answers the mod's actual problem.** Ids, `<#id>` and plain words all work, matched through this guild's emoji/pipe decoration and accents ("general" → `💬│general`, "foro poesia" → `💬│foro-poesía`). Unresolvable queries are **never dropped silently or guessed**: `ambiguous` ("foro" naming two), `not_sendable` (exists, bot can't write) and `unknown` are distinct because they need opposite answers from the mod, and each carries the sentence the model should say. Fan-out is capped at `MAX_BROADCAST_CHANNELS` (5) — a governor on blast radius, not performance.
   - **Forums are targets too, and post as posts** (the live miss: the first verify run answered _"no encontré ningún canal con ese nombre"_ for `foro poesia`, because a forum takes no loose messages and so was excluded from the candidate list entirely). `BroadcastChannel.kind` carries `text`/`forum` from resolution all the way into the stored draft — re-deriving it at send time could disagree with the preview — and a forum target is posted as a **new forum post** whose title is deterministic (`forumPostTitle`: event title + date, cut on a word inside Discord's 100-char cap). Thread creation has no `nonce`, so exactly-once there is approximated by looking for an active post with that exact title before creating.
   - **Mentions on a community-only post (general / anuncios) default to the 10:00 list — live: Usuarix `1436225305898389604`, never `@everyone`** (v1.28.0; names since v1.24.1). Same role the morning `#anuncios` post already pings; it is already in `calendar_settings.announce_mentions_json` / `set_announce_mentions`. Darko's 2026-08-31 extra post to general went out silent (`mentions:0`) while the 10:01 anuncios post had pinged Usuarix. `resolveAnnouncementMentions`: empty `mentions` + every target is general/anuncios → that allowlist, **with the `everyone` token stripped** so an extra post cannot escalate to `@everyone` even if that token is allowed for the morning run; a foro/eventos target, or a mixed fan-out (general + foro), stays silent unless they asked; `"nadie"` / `"sin ping"` opts out. A ping they *do* ask for is still only honoured for roles inside `set_announce_mentions`. Administrator on the bot is **not** the gate: Discord would let an admin ping anyone; the setting is. The earlier live miss (2026-08-18, _"perfecto usa el rol usuarix"_ → the bot invented "ese rol no está en la lista") was two holes, neither of them Discord: (1) the prompt never listed which roles ARE allowed, so the model passed `mentions: []` and then fabricated the refusal; (2) `resolveBroadcastMentions` treated any non-snowflake as junk, so even `"usuarix"` would have been refused. Names still resolve through the same fold as mod-role matching (`normalizeRoleName`), the prompt lists the allowed roles by name+id, and the draft payload carries `mentions: [{id, name}]`. The model is still forbidden from _writing_ mentions in the body (`stripModelMentions`); `allowedMentions` gates `@everyone` but does not stop it _rendering_.
   - **The mod's own words are the point.** `instruction` ("que diga bandaaaa, para que desempolven sus libretas") is given real weight in the writer prompt — it's the reason they asked instead of waiting — bounded by the same don't-invent-facts rule. The event link is appended deterministically, like the daily post; the flyer is **not** attached (the URL embed already shows the cover). There is **no template fallback**: a failed/stub model reply refuses rather than publishing a bad community post (the opposite trade-off from the daily announcer, where a plain heads-up beats silence).
   - **Verify:** `npx vitest run src/capabilities/calendar` (`broadcast.test.ts` partitioning/mentions — including the live `"usuarix"` name match, the general→Usuarix default, and the `@everyone` strip on that default — /stripping/composition/timing/tokens/`forumPostTitle`, `broadcast-channels.test.ts` the matcher against this guild's real decorated names incl. forums and the sendable/unknown/ambiguous split, `broadcast-tools.test.ts` the full draft→confirm→post flow over real SQLite: drafting posts nothing, `publish_now` posts in the same call and prefixes Usuarix not `@everyone`, the sent text is the stored text, the token is single-use, a garbage/missing token falls back to this channel's pending draft, a forum becomes a titled post, `"usuarix"` prefixes the ping, partial fan-outs report which channels failed, non-mods are refused both in the bundle and server-side, `announce-store.test.ts` the draft table incl. a legacy target row degrading to `text`) plus the live, read-only `npx tsx scripts/verify-calendar-broadcast.ts <id> <canales…> [--say "…"]` — it drives the REAL tools against the live DB/guild/LLM and prints the resolved channels and the exact message it _would_ post; `--send` to actually publish (and it then proves the single-use guarantee against the live DB). Journal: `calendar.broadcast.post_failed`, `calendar.broadcast.forum_post_exists`, `calendar.broadcast.write_failed`, `calendar.broadcast.send_no_draft` / `send_already_posted` / `send_expired`, and the `tool_call` lines for both tools (`token`, `channels`, `forums`, `problems`, `mentions`, `mentions_defaulted`, `publish_now`).
- **`calendar_sync_discord_event`** (mod tool, added 2026-08-05; grew a full lifecycle 2026-08-06, v1.9.0): creates the Discord scheduled event for a calendar row and links the two, so the announcement can carry a clickable RSVP. Idempotent (an already-linked live event is returned, never duplicated); for a series it schedules the **next occurrence** rather than translating our recurrence into Discord's.
   - **Venue resolution** (v1.28.0 default): free-text `location` fuzzy-matches voice/stage channels the bot can see (→ a Voice/Stage event with a join button); with NO location it **infers the room from the event title** (`resolveVenue` — the room's significant phrase, stopwords like "sala/de" dropped, must appear contiguously in the title: "… | Club de poesía" → Sala de Club de Poesía; conservative on purpose, a wrong room is worse than none); **else it lands in Sala de Eventos** when that room exists (ticket-0007 said "sala de eventos", the model created without `location`, title inference missed, Discord event was EXTERNAL). Only if that room is missing does it become an `External` event. Reaching Discord is injected as a `DiscordEventSyncer` (`createEventSyncer`), so the tool layer stays discord.js-free and the calendar prompt can offer it once after a create.
   - **Public copy drops Agitprop/flyer credits** (v1.28.0, `publicEventDescription`). Calendar `description` is copied onto the Discord event card and into the announcement writer brief; production credits ("🎨 Flyer a cargo de la Comisión de Agitprop", "Flyer: hará el solicitante") stay in the ticket, not on the community card. Live #37 leaked the credit onto the Discord embed.
   - **The link is a LIFECYCLE, not a one-shot create** (the v1.9.0 core). `DiscordEventSyncer` also has `refresh(eventId)` and `remove(eventId)`, and `CalendarToolSource.handleUpdate`/`handleDelete` call them automatically (`propagateToDiscordEvent`): **editing a linked calendar event edits the Discord event** (title/description/time, plus the room only when one is positively resolved — an unresolvable location never strips a hand-picked room, and a refresh never touches a banner a mod set by hand), and **deleting a series deletes the Discord event** (remove runs BEFORE `store.delete`, because the link lives on the row). Occurrence-scope edits/deletes refresh the master (the Discord event reflects the next occurrence). Only real outcomes reach the tool payload (`discord_event: {action: 'updated'|'deleted', …}` or an `error` note) so the model can confirm "también actualicé el evento de Discord" — unlinked rows and no-op refreshes stay silent, and a Discord-side failure never fails the calendar op. This came from a live refusal (2026-08-05): a mod moved "Rosario Castellanos" and the bot could only say "edit the Discord one by hand".
   - **"Crea el evento" is ambiguous, and the fix was mostly context, not prompting** (v1.16.0, from the live 2026-08-10 exchange). An admin replied to the nudge above with _"crea el evento"_ and the bot asked for a title and a start date — for an event it had itself just named. Three separate holes, each fixed where it actually was:
      1. **The framework threw the referent away.** `normalizeTurns` dropped leading assistant turns, and a nudge is always one → the model saw a bare `"crea el evento"` (`historyTurns: 1`, `toolCalls: 0`). It now folds them in as quoted context — see [framework.md](../framework.md) step 3. This was never a calendar bug; it silently affected every bot-initiated thread.
      2. **The snapshot didn't carry the state the question is about.** Each line of "Próximos eventos" now ends in `evento de Discord: ✅` / `❌ FALTA` (and `@ (sin sala)` when there's no location), so "which one is missing its Discord event" is answerable _before_ any tool call, and the prompt can say "if exactly one is ❌, it's that one".
      3. **The prompt had no disambiguation rule.** A "Crea el evento a secas — cuál de los dos" section orders the search for a referent: quoted context → this conversation → the single ❌ in the snapshot; asking "¿qué evento?" is the last resort, and **never** ask for title + date for something already in the calendar. A genuinely new title + date is still a `calendar_create_event`.
   - **The roomless fallback is no longer silent** (v1.16.0; default room v1.28.0). `resolveVenue` is conservative on title inference, then defaults to **Sala de Eventos**. `needs_room: true` is only when that room (and every other match) is missing — then it becomes an `External` event at "Revolución Z" with no join button. Live, #29 and ticket-0007 (#36/#37) were created EXTERNAL and confirmed as if finished. `sync`/`refresh` return an `EventVenue` (`voice`/`stage`/`external` + name), surfaced in the tool payload as `venue_kind`/`venue_name`/`needs_room`; the prompt makes the bot name the room it used, or say it found none and ask which sala it is — once. Answering stores the `location`, and the existing refresh propagation **moves the Discord event into that room**. That repair needs an `EXTERNAL → VOICE/STAGE` conversion, which Discord wants `entity_metadata: null` for and **discord.js's `edit()` silently drops** (its `create()` sends it — the asymmetry is in the library, not the API), so a rejected edit retries once through `client.rest.patch` with the documented body (`convertToVenue`, logged as `calendar.discord_events.convert_venue_failed` when even that fails).
   - **Cover image (banner) support.** The sync tool takes an optional `image_url`, validated against `allowedImageUrls` — exactly the image attachment URLs advertised in that turn's system prompt (`CapabilityTurnContext.attachments`, populated by `handlers.ts` from `listImageAttachments()`, so a hallucinated/injected URL is refused before any fetch). The syncer downloads it via `fetchDiscordCdnImage` (host allowlist = Discord CDN only, mime + 10 MB caps — SSRF guard) and sets it as the event cover (base64 data URI; works on create AND on an already-linked event, so "ponle esta portada" is the same call). A failed download never blocks the event itself. Since v1.19.1 the attachments prompt section says so explicitly (it works on an EXISTING event — the cover is replaced, the link unchanged): a mod attaching the flyer and saying "con esta imagen" should map straight to the sync call.
- **Previewing the board before it posts** (`scripts/preview-calendar-board.ts`, read-only, added 2026-08-03): prints the current vs desired card set, exactly which cards would be **posted and DELETED**, the rollover verdict, and the range status of every recurring series. `--render` also writes the real PDF+PNG to `/tmp/chopper-cal` so the card can be eyeballed. Run it before a restart/publish — a reconcile _deletes_ community messages, so that should never be a surprise.
- **Live ops scripts (added 2026-08-13, all drive the production code paths against the LIVE DB + guild):** `scripts/move-calendar-event.ts <id> <YYYY-MM-DD> <HH:MM>` re-anchors an event/series to a new local start (shifts `end_at`/`recurrence_until` by the same delta) and republishes; `scripts/delete-calendar-event.ts <id> [--also-delete-message ch/msg]` removes the linked Discord event FIRST, then the row, then republishes (the same order as the capability's delete handler); `scripts/calendar-tool.ts <toolName> '<json>' [--sync-discord]` pipes a raw tool payload through the real `CalendarToolSource` (occurrence overrides, same-day retime guard and all) and can follow with `calendar_sync_discord_event`; `scripts/announce-upcoming-event.ts <id> [--date …] [--post]` posts an **advance** announcement the daily announcer structurally can't (it only handles the day of, and skips occurrences that already started) — it uses `renderAnnouncementPrompt`'s `'advance'` framing, never writes the ledger, so the day-of auto-announcement still fires. Preview-first: the announce script posts nothing without `--post`.
- **Proof scripts** (not tests, write to `/tmp/chopper-cal`): `scripts/render-calendar-sample.ts` (renders the real example events + stress cases to PDF/PNG/ICS), `scripts/calendar-e2e-proof.ts` (drives the real tool handlers against an on-disk DB, reopens it to prove persistence, renders the final state, validates the ICS), and `scripts/calendar-bedrock-smoke.ts` (drives the real capability + `ask()` against the **real text model** through 10 replayed mod conversations — Scenes 8–10 are the bounded-series guards; Scene 2's occurrence-override check is a soft `~` warning, i.e. model-behavior variance, not a gate). Verify renders visually with `pdftoppm -png -r 150 <pdf>`.
- **Test speed gotcha:** `__tests__/rollover.test.ts` drives the real publisher but **mocks `raster.js`** — real `pdftoppm` at 150 DPI costs ~5 s per card on the Pi versus ~100 ms for the pdf-lib render, so un-mocking it takes the file from 1.5 s to 55 s. The real rasterizer is covered by `preview-calendar-board.ts --render` instead.
- **Wiring the test deployment:** set `CALENDAR_OUTPUT_CHANNEL_ID` in `.env` (done: the Revolución Z output channel), then from the config channel bind the input channel to `calendar` (`config_bindings action:bind`). `pnpm run build && systemctl --user restart chopperbot.service` to pick up the env/output-channel change.
